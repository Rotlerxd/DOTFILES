general {
    output_format = "i3bar"
    colors = false
    markup = pango
    interval = 1
    color_good = "#2f343f"
        color_degraded = "#ebcb8b"
        color_bad = "#ba5e57"
    tray_padding = 2
}

order += "vol"
order += "load"
order += "memory"
order += "battery all"
order += "tztime local"

load {
    format = "%5min Load "
}

battery all {
    format = "<span> %status %percentage </span>"
}

vol {

}

memory {
    format = "<span> %used / %total RAM </span>"
}

tztime local {
    format = " %a %-d %b %H:%M:%S "
}





